import { Layout } from 'antd'
import React from 'react'
const { Header } = Layout

const App: React.FC = () => (
  <Header className="header">
    <div className="logo" />
  </Header>
)

export default App
